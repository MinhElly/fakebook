logging:
  level:
    com.minh.fakebook.media: DEBUG
    org.hibernate.SQL: DEBUG

server:
  port: 8084

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mariadb://localhost:${MARIADB_PORT:3307}/mediaservice?useLegacyDatetimeCode=false
    username: ${FAKEBOOK_DB_USERNAME:root}
    password: ${FAKEBOOK_DB_PASSWORD:}
  liquibase:
    contexts: dev, faker

cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:twjav6q1}
  api-key: ${CLOUDINARY_API_KEY:931967512613569}
  api-secret: ${CLOUDINARY_API_SECRET:kvUZixEIPrpELyFDU5nXqG8TX-A}
