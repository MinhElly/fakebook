logging:
  level:
    com.minh.fakebook.user: DEBUG
    org.hibernate.SQL: DEBUG

server:
  port: 8082

spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    url: jdbc:mariadb://localhost:${MARIADB_PORT:3307}/userservice?useLegacyDatetimeCode=false
    username: ${FAKEBOOK_DB_USERNAME:root}
    password: ${FAKEBOOK_DB_PASSWORD:}
  liquibase:
    contexts: dev
